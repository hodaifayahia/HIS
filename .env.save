


# Application
APP_NAME=HIS
APP_ENV=local
APP_KEY=base64:oQ13yPmK/PgIGrBrRsmXSR1R0oWTt95FySwPp79Kdc0=
APP_DEBUG=true
APP_TIMEZONE=Africa/Algiers
APP_URL=http://localhost

# Localization (French for Algeria)
APP_LOCALE=fr
APP_FALLBACK_LOCALE=fr
APP_FAKER_LOCALE=fr_FR

# Maintenance
APP_MAINTENANCE_DRIVER=file

# PHP Configuration
PHP_CLI_SERVER_WORKERS=4
BCRYPT_ROUNDS=12

# Logging
LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Database (Docker MySQL)
DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=his_database
DB_USERNAME=sail
DB_PASSWORD=password

# Session (Redis for better performance)
SESSION_DRIVER=redis
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

# Broadcasting (Remove Reverb for now)
BROADCAST_CONNECTION=log
BROADCAST_DRIVER=log

# Filesystem
FILESYSTEM_DISK=local

# Queue (Redis for better performance)
QUEUE_CONNECTION=redis

# Cache (Redis for Docker)
CACHE_STORE=redis
CACHE_PREFIX=

# Redis (Docker service)
REDIS_CLIENT=phpredis
REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379

# Mail (Mailpit for development)
MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="noreply@his-system.local"
MAIL_FROM_NAME="${APP_NAME}"

# Meilisearch (Search engine)
SCOUT_DRIVER=meilisearch
MEILISEARCH_HOST=http://meilisearch:7700
MEILISEARCH_KEY=your-master-key-here
MEILISEARCH_NO_ANALYTICS=true

# Docker Sail Configuration
WWWGROUP=1000
WWWUSER=1000

# Port Configuration
APP_PORT=80
VITE_PORT=5173
FORWARD_DB_PORT=3306
FORWARD_REDIS_PORT=6379
FORWARD_MEILISEARCH_PORT=7700
FORWARD_MAILPIT_PORT=1025
FORWARD_MAILPIT_DASHBOARD_PORT=8025

# MySQL Extra Options (fixes warning)
MYSQL_EXTRA_OPTIONS=--default-authentication-plugin=mysql_native_password

# Sail Xdebug (for debugging)
SAIL_XDEBUG_MODE=off
SAIL_XDEBUG_CONFIG=client_host=host.docker.internal

# Vite Configuration
VITE_APP_NAME="${APP_NAME}"
VITE_APP_URL="${APP_URL}"

# AWS (if needed later)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false
